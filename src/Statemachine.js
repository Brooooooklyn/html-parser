export class StateMachine {
  constructor(prev, pos) {
    this.prev = prev;
    this.pos = pos;
  }

  getTags() {
    switch(this.pos) {
      case -1:

        break;
      case 6:

        break;
    }
  }

  getNodeBegin() {
    switch(this.pos) {
      case -1:

        break;
      case 1:

        break;
      case 5:

        break;
    }
  }

  buildNode() {
    switch(this.pos) {
      case -1:

        break;
      case 0:

        break;
    }
  }

  getEndNode() {
    switch(this.pos) {
      case -1:

        break;
      case 5:

        break;
      case 6:

        break;
    }
  }

  getAttributesKey() {
    switch(this.pos) {
      case -1:

        break;
      case 2:

        break;
    }
  }
}